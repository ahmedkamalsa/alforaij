# Alforaij Research Assistant - Project Context

## الهدف

تطبيق مساعد بحث وتقييم عقاري عربي لشركة الفريج. الهدف العملي هو مساعدة الموظف على:

- فهم طلب العميل من نص حر.
- البحث داخل بيانات الفريج والمصادر الخارجية المتاحة.
- ترتيب النتائج حسب مطابقة الطلب، جاذبية السعر، جودة البيانات، وثقة المصدر.
- تفسير كل رقم ظاهر: السعر، المساحة، الوسيط، نسبة السعر، الثقة، والتوصية.
- حفظ التقرير وسجل البحث في Supabase عند توفر الاتصال.

## قاعدة مهمة عن المساحة

لا يجوز اعتبار أرقام الارتداد أو عرض الشارع أو الواجهة مساحة عقار.

المساحة تدخل في التقييم فقط إذا كانت مذكورة كمساحة فعلية للعقار أو الأرض. إذا كان الإعلان يذكر مثلًا "ارتداد 40 متر" فقط، فالمساحة تعتبر غير مذكورة.

## مصادر البيانات

- الفريج: المصدر المحلي الأساسي، محمل من بيانات لوحة `alforaijboard`.
- OpenSooq / Mourjan / Q8Aqar / 4Sale / Sakan / Aqarat / Bu3qar / Waseet / NabdAqar: مصادر خارجية حسب ما تسمح به صفحاتها العامة أو بدائل البحث.
- الصفقات الرسمية والمؤشرات الرسمية: مرجع أعلى مصداقية عند توفر بيانات منظمة في Supabase أو الملفات المحلية.

## مصادر لا يجوز الادعاء بأنها مرتبطة بالكامل إلا بعد إدخال بياناتها

المنصة تدعم أماكن هذه المصادر في التصميم والقاعدة، لكن لا تعتبرها مصدر تقييم فعليًا إلا إذا رجعت بيانات منظمة داخل التقرير:

- وزارة العدل / التسجيل العقاري: تدخل التقييم فقط عند توفر صفقات موثقة بمنطقة، نوع عقار، تاريخ، مساحة، وسعر.
- PACI / Kuwait Finder: تستخدم لتأكيد المنطقة والموقع والقطعة عند توفر ربط أو ملف منظم، ولا تعوض سعر الصفقة.
- البنوك وشركات التمويل: تستخدم كمؤشرات تمويل أو تقارير سوقية عند توفر ملف/رابط/جدول واضح؛ لا تحسب كتقييم رسمي منفرد.
- الإعلانات الخارجية: تدخل المقارنات فقط إذا كانت نفس المنطقة ونفس نوع العقار ونفس العملية، وبها سعر قابل للقراءة.

## سياسة الاعتماد على المصادر

ليست كل صفحة مفتوحة تعتبر مصدر تقييم.

- `دخل التقييم`: المصدر أعاد بيانات منظمة قابلة للمقارنة.
- `دخل عبر بديل`: المصدر الأصلي تعذر جزئيًا لكن بديل موثق أعاد نتائج فعلية.
- `مساعد فقط`: رابط البحث أو الصفحة متاح، لكن لا توجد بيانات إعلان منظمة تكفي للحساب.
- `لا يدخل التقييم`: لا نتائج أو لا بيانات وقت البحث.
- `فشل الاتصال`: لا يستخدم في التقييم.

## طريقة الترتيب

الترتيب ليس حسب الثقة وحدها. درجة التوصية تعتمد على:

- مطابقة الطلب: المنطقة، نوع العقار، العملية، المساحة، الميزانية.
- جاذبية السعر: السعر مقابل وسيط المقارنات أو المرجع الرسمي.
- الثقة: عدد وجودة المقارنات والمصدر.
- جودة البيانات: اكتمال السعر، المساحة، الرابط، المنطقة، والمقارنات.
- خصم البيانات الناقصة أو النتائج خارج المنطقة المطلوبة.

## التصنيف القانوني والتمويلي

كل نتيجة تحتوي `propertyProfile` عند الإمكان:

- نوع الأصل: بيت، شقة، أرض/قسيمة، عمارة، أو عقار غير محدد.
- الملكية/النطاق: حكومي/رعاية سكنية، سكن خاص، أو غير محدد.
- الاستخدام: سكني، استثماري، تجاري/صناعي.
- الحالة التمويلية: بنك الائتمان/الإسكان، يحتاج تحقق بنكي، أو غير مذكور.
- الحالة القانونية: وثيقة/شهادة أوصاف مذكورة، أو غير مذكور.

هذا التصنيف مستخرج من نص الإعلان فقط. لا يتحول إلى حقيقة رسمية إلا بوجود وثيقة أو مصدر رسمي موثق داخل التقرير.

## البيع مقابل الإيجار

خط البيع:

- السعر الإجمالي.
- سعر المتر عند توفر المساحة.
- وسيط أسعار المقارنات.
- نسبة السعر للوسيط أو للقيمة الرسمية.
- تمويل عقاري تقديري عند توفر السعر.

خط الإيجار:

- الإيجار الشهري.
- الإيجار السنوي.
- إيجار المتر شهريًا عند توفر المساحة.
- وسيط إيجارات المنطقة.
- عائد إيجاري سنوي تقديري عند توفر قيمة عقار مرجعية.

لا يتم خلط سعر البيع مع الإيجار الشهري.

## Supabase

الجداول الأساسية المتوقعة:

- `saved_reports`: التقرير الكامل.
- `source_runs`: سجل تشغيل كل مصدر لكل طلب.
- `listing_evidence`: مصدر كل رقم داخل النتيجة.
- `opportunities`: لقطات الفرص اليومية.
- `outreach_clicks`: تتبع نسخ/إرسال رسائل واتساب.
- `search_history`: سجل بحث مختصر للتحليلات المستقبلية.

غياب `search_history` لا يكسر التحليل؛ يتم حفظ التقرير الكامل في `saved_reports` إذا كان متاحًا.

## أوامر التشغيل

```powershell
python -m backend.main
```

الرابط المحلي:

```text
http://127.0.0.1:8000/
```

فحص الصحة:

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/health
```

تشغيل الاختبارات:

```powershell
$env:PYTHONDONTWRITEBYTECODE='1'; python -m unittest discover -s tests -v
```

## قاعدة التعامل مع التعديلات

قبل أي تعديل كبير:

1. راجع `PROJECT_CONTEXT.md`.
2. راجع `backend/services/report_generator.py` لمعرفة شكل التقرير.
3. راجع `backend/services/valuation.py` لمنطق البيع/الإيجار.
4. راجع `backend/connectors/live_sources.py` للمصادر الخارجية.
5. شغل الاختبارات بعد التعديل.
